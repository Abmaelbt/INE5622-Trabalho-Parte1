def verifica_maior(int x, int y, int z) {
    if (x > y) {
        return x;
    } else if (x < y) {
        return y;
    } else {
        return z;
    }
}

def main() {
    int x, y, z;
    x = 20;
    y = 20;
    z = 30;

    print verifica_maior(x, y, z);
}
